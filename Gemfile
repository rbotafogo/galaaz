# A sample Gemfile
source "https://rubygems.org"

# Security: RDoc < 6.1.2.1 allowed arbitrary code via | in filenames (GHSA advisory).
# Also declared in galaaz.gemspec as a development dependency; kept here so manifest scanners
# and Dependabot see a direct requirement at the repo root.
gem "rdoc", ">= 6.1.2.1", group: :development

gemspec